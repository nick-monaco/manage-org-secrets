{
  "name": "manage-secrets",
  "author": "Nick Monaco",
  "version": "1.0.0",
  "main": "index.js",
  "description": "Github Action to save a repository, organization or dependabot secret.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nick-monaco/manage-org-secrets.git"
  },
  "scripts": {
    "prepare": "ncc build index.js -o dist --source-map"
  },
  "dependencies": {
    "@actions/core": "^1.9.0",
    "@actions/github": "^5.0.3",
    "tweetsodium": "^0.0.6"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.34.0"
  }
}
